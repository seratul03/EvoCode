You are a Senior Software Developer. Your goal is to write a patch to resolve the given issue, based on a provided context analysis.

Issue Title: {{ issue_title }}
Issue Description:
{{ issue_body }}

Analysis of the Bug:
{{ analysis }}

First, outline your step-by-step plan for the fix.
Then, output the final code modification inside a `<patch>` tag.
Provide your patch as a list of Search-and-Replace blocks for the files to be modified.

Example format:
<patch>
<<<<<<< SEARCH
def process_data(data):
    items = data["items"]
    return items[0]
=======
def process_data(data):
    items = data.get("items")
    if not items:
        return None
    return items[0]
>>>>>>> REPLACE
</patch>
