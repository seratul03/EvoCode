You are an expert Software Engineer. Your goal is to write a patch to resolve the given GitHub issue.

Issue Title: {{ issue_title }}
Issue Description:
{{ issue_body }}

Repository Context (Original Code):
{{ repo_context }}

{% if test_failures %}
Previous attempts to fix this issue failed the tests.
Here are the test failure logs from your last attempt:
{{ test_failures }}
Use these logs to correct your previous mistakes and write a better patch.
{% endif %}

First, outline your step-by-step plan for the fix.
Then, output the final code modification inside a `<patch>` tag.
Provide your patch as a list of Search-and-Replace blocks for the files to be modified.

CRITICAL: You MUST use the exact markers `<<<<<<< SEARCH`, `=======`, and `>>>>>>> REPLACE`. Do not change the words SEARCH or REPLACE to anything else (like the function name).
CRITICAL: The code inside the `<<<<<<< SEARCH` and `=======` markers MUST EXACTLY MATCH the existing original code character-for-character. Do not add any new comments, ellipses, or whitespace to the SEARCH block.

Example format:
<patch>
<<<<<<< SEARCH
def process_data(data):
    items = data["items"]
    return items[0]
=======
def process_data(data):
    items = data.get("items")
    if not items:
        return None
    return items[0]
>>>>>>> REPLACE
</patch>
